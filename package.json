{
  "name": "sonicdrop-p2p",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node --optimize-for-size --max-old-space-size=512 server.js",
    "prod": "NODE_ENV=production node --turbo-fast-api-calls server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "peer": "^1.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}